# Application Settings
APP_NAME=PromptForum
APP_VERSION=1.0.0
DEBUG=True

# Security Settings (IMPORTANT: Generate a secure random secret key)
# You can generate one using: openssl rand -hex 32
SECRET_KEY=your-super-secret-key-here-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Database Settings
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=promptforum

# Email Settings (SMTP Configuration)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com
EMAIL_FROM_NAME=PromptForum

# OTP Settings
OTP_EXPIRE_MINUTES=10
OTP_LENGTH=6

# Google OAuth Settings
# Get these from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# Frontend URL
FRONTEND_URL=http://localhost:3000

# ===============================
# Payment Gateway Settings
# ===============================

# Cashfree Payment Gateway
# Get credentials from: https://merchant.cashfree.com/
CASHFREE_APP_ID=your-cashfree-app-id
CASHFREE_SECRET_KEY=your-cashfree-secret-key
CASHFREE_SANDBOX=true
CASHFREE_API_VERSION=2023-08-01

# Platform Fees (optional)
PAYMENT_PLATFORM_FEE_PERCENTAGE=0
PAYMENT_PLATFORM_FEE_FIXED=0

# Cashfree Gateway Fees (for display only, actual fees charged by gateway)
CASHFREE_GATEWAY_FEE_PERCENTAGE=2
CASHFREE_GATEWAY_FEE_FIXED=0

# ===============================
# Withdrawal Settings
# ===============================
# NOTE: Withdrawal configuration is now FULLY DYNAMIC from database!
# Run: python seed_withdrawal.py to initialize config and methods
#
# All settings are stored in MongoDB collections:
#   - withdrawal_config: Global settings (limits, fees, currencies, etc.)
#   - withdrawal_methods: Available payment methods with their fees
#
# Modify settings via:
#   1. MongoDB Compass / mongosh directly
#   2. Admin API endpoints (if implemented)
#   3. Re-run seed_withdrawal.py after editing the file

# ===============================
# Contest Fee Settings
# ===============================
# NOTE: Contest fee configuration is FULLY DYNAMIC from database!
# Run: python seed_contest_config.py to initialize config
#
# All settings are stored in MongoDB collections:
#   - contest_config: Global contest fee settings
#
# Features:
#   - Platform fee on contest creation (% or fixed)
#   - Prize pool locking (credits held until contest ends)
#   - Min/max fee caps
#   - Contest limits per user
#   - Refund policy on cancellation
#
# Fee Types:
#   - "percentage": Prize pool * fee_percentage%
#   - "fixed": Flat fee amount
#   - "mixed": fixed + (prize * percentage)
#
# Example (5% fee with 10 min, 1000 max):
#   Prize 100 → Fee 10 (min applies)
#   Prize 5000 → Fee 250 (5%)
#   Prize 50000 → Fee 1000 (max applies)
#
# Modify settings via:
#   1. MongoDB Compass / mongosh directly
#   2. Admin API endpoints (if implemented)
#   3. Re-run seed_contest_config.py after editing the file
